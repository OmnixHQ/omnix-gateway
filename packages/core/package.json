{
  "name": "@ucp-gateway/core",
  "version": "0.1.0",
  "private": true,
  "description": "UCP engine — types, routing, normalisation, and adapter interfaces",
  "license": "Elastic-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@ucp-js/sdk": "^0.1.0",
    "drizzle-orm": "^0.30.0",
    "ioredis": "^5.4.0",
    "jose": "^6.2.2",
    "postgres": "^3.4.0"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "typescript": "*"
  }
}
