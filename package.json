{
  "name": "ucp-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "Universal Commerce Protocol gateway — connect any e-commerce store to any AI agent",
  "license": "Elastic-2.0",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/core && npm run build -w packages/adapters && npm run build -w apps/server",
    "dev": "npm run dev -w apps/server",
    "test": "npm run test --workspaces --if-present",
    "lint": "eslint . --ext .ts,.tsx",
    "lint:fix": "eslint . --ext .ts,.tsx --fix",
    "format": "prettier --write \"**/*.{ts,tsx,js,json,md}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,json,md}\"",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "clean": "npm run clean --workspaces --if-present && rm -rf node_modules",
    "validate:ucp": "node --experimental-transform-types scripts/validate-ucp-compliance.ts",
    "test:e2e:magento": "bash tests/e2e-magento/run-e2e-checkout.sh",
    "test:e2e:magento:vitest": "vitest run --config tests/e2e-magento/vitest.config.ts --dir tests/e2e-magento",
    "test:e2e:magento:setup": "bash tests/e2e-magento/setup-magento.sh && bash tests/e2e-magento/seed-tenant.sh"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "eslint": "^8.57.0",
    "eslint-config-prettier": "^9.1.0",
    "prettier": "^3.3.0",
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=22"
  }
}
